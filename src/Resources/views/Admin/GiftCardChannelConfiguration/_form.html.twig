{% import '@SyliusUi/Macro/messages.html.twig' as messages %}

<div class="ui grid">
    <div class="eight wide column">
        <div class="ui segment">
            {{ form_row(form.code) }}
            {{ form_row(form.enabled) }}
        </div>
    </div>
    <div class="eight wide column">
        <div class="ui segment">
            {{ form_row(form.channel) }}
        </div>
    </div>
</div>
<div class="ui segment">
    <div class="ui grid">
        <div class="eight wide column">
            {{ form_row(form.expirationDelay) }}
        </div>
        <div class="eight wide column">
            {% set test %}

            {% endset %}

            <div class="ui icon info message">
                <i class="info circle icon"></i>
                <div class="content">
                    <div class="header">{{ 'sylius.ui.info'|trans }}</div>
                    <p>{{ 'macbim_sylius_gift_cards.form.gift_card_channel_configuration.expiration_delay_help'|trans|raw }}</p>
                </div>
            </div>
        </div>
    </div>
</div>
